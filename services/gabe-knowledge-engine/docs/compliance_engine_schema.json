{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ComplianceEngineManualAnswer",
  "type": "object",
  "required": [
    "answer",
    "source_type",
    "manual_title",
    "page_number",
    "source_url",
    "quote",
    "confidence",
    "certainty",
    "run_outcome"
  ],
  "properties": {
    "answer": { "type": "string" },
    "source_type": { "const": "manual" },
    "manual_title": { "type": "string" },
    "page_number": { "type": "integer", "minimum": 1 },
    "source_url": { "type": "string", "format": "uri" },
    "quote": { "type": "string", "minLength": 1 },
    "confidence": { "type": "number", "minimum": 0, "maximum": 100 },
    "certainty": {
      "type": "string",
      "enum": ["Verified Exact", "Verified Partial", "Interpreted", "Unverified"]
    },
    "run_outcome": {
      "type": "string",
      "enum": ["approved_compliance", "refused_missing_explicit_support", "rejected"]
    },
    "validator_notes": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
